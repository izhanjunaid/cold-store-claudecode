node_modules/
dist/
.next/
.turbo/
.env
.env.local
.env.production
.env.production.local
*.log

# Production deploy (Phase 13) — secrets, client PII, local artifacts
backups/
logs/
# update.ps1 keeps the previous release's deploy files next to the live ones so a
# failed update can put the pair back together (see phases/phase-26-update-pipeline.md).
*.prev
update.ps1.new
/client-config.json
/client-config.*.json
test-results/
.playwright-cli/
cloudflared.exe
cloudflared.out
ngrok.out
coverage/
.DS_Store
*.tsbuildinfo
.claude/settings.local.json
packages/db/prisma/seed.js
packages/db/prisma/seed.d.ts
packages/db/prisma/seed.js.map
packages/db/prisma/seed.d.ts.map

# graphify knowledge-graph output (generated; rebuild with `graphify update .`)
graphify-out/
